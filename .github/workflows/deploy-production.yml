name: Deploy - Production

on:
  push:
    branches:
      - release/production

jobs:
  build:
    name: Build & Deploy
    runs-on: ubuntu-latest
    environment: production
    concurrency: deployment
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache bun dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Cache Next.js build
        uses: actions/cache@v4
        with:
          path: |
            .next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/bun.lockb') }}-${{ hashFiles('**/*.js', '**/*.jsx', '**/*.ts', '**/*.tsx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/bun.lockb') }}-

      - name: Install Dependencies
        run: bun install --frozen-lockfile

      - name: Build Package
        run: bun run build
        env:
          # Environment-specific variables (from production environment)
          S3_BUCKET_NAME: ${{ vars.S3_BUCKET_NAME }}
          NEXT_PUBLIC_APP_URL: ${{ vars.NEXT_PUBLIC_APP_URL }}
          AWS_REGION: ${{ vars.AWS_REGION }}

          # Environment-specific secrets
          BETTER_AUTH_SECRET: ${{ secrets.BETTER_AUTH_SECRET }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          API_KEY: ${{ secrets.API_KEY }}

          # Shared infrastructure secrets
          AWS_ACCESS_KEY_ID: ${{ secrets.DEPLOY_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.DEPLOY_AWS_SECRET_ACCESS_KEY }}

          # SES Configuration (if enabled)
          EMAIL_SERVER_HOST: ${{ vars.EMAIL_SERVER_HOST }}
          EMAIL_SERVER_PORT: ${{ vars.EMAIL_SERVER_PORT }}
          EMAIL_FROM: ${{ vars.EMAIL_FROM }}
          EMAIL_SERVER_USER: ${{ vars.EMAIL_SERVER_USER }}
          EMAIL_SERVER_PASSWORD: ${{ secrets.EMAIL_SERVER_PASSWORD }}

          # SMS/Twilio Configuration (optional)
          SMS_BASE_URL: ${{ vars.SMS_BASE_URL }}
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_MESSAGING_SERVICE_SID: ${{ secrets.TWILIO_MESSAGING_SERVICE_SID }}
          ENABLE_SMS: ${{ vars.ENABLE_SMS }}

          # Project-specific environment variables
          NEXT_PUBLIC_DB_ENV: production
          DATABASE_URL: file:./db.sqlite

      - name: Deploy to Server
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avzr --exclude=".git" --exclude="node_modules/.cache" --exclude="db.sqlite" --exclude="*.db"
          remote_path: /srv/www/production/releases/${{ github.sha }}/
          remote_host: ${{ secrets.EC2_HOST }}
          remote_user: ubuntu
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Activate Deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            /usr/local/bin/deploy.sh production ${{ github.sha }}
